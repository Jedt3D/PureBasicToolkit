# PureBasic Toolkit v0.8.0

## Suggested PR Title

`feat: add workspace symbol search baseline`

## Suggested Tag

`v0.8.0`

## Suggested PR Body

## Summary

- add workspace symbol search for core PureBasic declarations
- broaden Go to Definition with a wider workspace-aware fallback
- activate earlier when the workspace already contains PureBasic files
- package and document the Phase 8 baseline as `0.8.0`

## Testing

- [x] `npm install --package-lock-only`
- [x] `npm run build`
- [x] `npm run package:vsix`
- [x] installed `purebasic-toolkit-0.8.0.vsix` in VS Code and confirmed `worajedt.purebasic-toolkit@0.8.0`
- [x] smoke-tested broader qualified-definition lookup with `findBestDefinitionMatch(...)` against include-style definitions
- [x] smoke-tested workspace-symbol ranking so exact `Math::Add` matches outrank broader prefix matches

## Suggested GitHub Release Title

`PureBasic Toolkit v0.8.0`

## Suggested GitHub Release Notes

# PureBasic Toolkit v0.8.0

## Added

- workspace symbol search for PureBasic procedures, modules, declarations, structures, enumerations, macros, and interfaces
- broader workspace-aware Go to Definition fallback after local and include-linked lookup
- activation when a workspace already contains PureBasic files

## Changed

- bumped the extension to its first Phase 8 workspace-symbol baseline
- expanded Marketplace metadata to reflect workspace symbol search support
- updated the README and release docs for the broader navigation workflow
